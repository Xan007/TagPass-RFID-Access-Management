# Frontend - Environment Configuration Template (.env.example)
# Copia este archivo a .env y rellena con tus valores reales
# El archivo .env debe estar en .gitignore y NUNCA subirse al repositorio

# Supabase Configuration
# Obtén estos valores del Supabase Dashboard → Project Settings → API keys
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=sb_publishable_your-anon-public-key

# IMPORTANTE: 
# - SUPABASE_KEY: Usa la clave "Publishable key" (también llamada "Anon key")
# - NUNCA uses la "Service Role key" en el frontend (es un riesgo de seguridad)
# - La "Service Role key" solo se usa server-side si es necesario

# Flask Configuration
# Generar clave secreta: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
FLASK_SECRET_KEY=generate-a-random-secret-key-here
DEBUG=false
PORT=5000

# Database Tables (reference, no need to configure)
# Estas tablas están creadas en Supabase:
# - access_events: eventos de acceso (card_uid, room_id, authorized, event_time)
# - access_blocks: bloqueos de acceso (card_uid, room_id, reason, blocked_by)
# - rfid_cards: tarjetas registradas (uid, person_name, student_code)
# - rooms: salones (id, name, building_id, type)
# - buildings: edificios (id, name)
# - users: usuarios del sistema (id, email, name, is_admin)
# - raspberry_devices: dispositivos Raspberry (id, name, location, room_id)
